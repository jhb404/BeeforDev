/**
 * Compatibility shim. Actions were split per domain into `src/automation/beefor/actions/`.
 * Public API (performLogin, doSelectMood, doFetchTimesheet, ...) preserved.
 */
export * from './actions';
