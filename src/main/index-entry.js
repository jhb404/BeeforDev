// Electron APIs are available globally in the main process
// This file serves as the entry point and imports the compiled TypeScript code
// The Electron APIs are already available globally, no need to require them

// Now import the compiled TypeScript code
require('./index.js');
