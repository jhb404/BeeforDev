/**
 * Compatibility shim. All types now live in `src/shared/types/` split by domain.
 * Existing imports `from '../shared/types'` continue to work via this re-export.
 */
export * from './types/index';
